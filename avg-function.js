// Calculating Average using Function

function make_avg(num1, num2, num3) {
    let avg = (num1 + num2 + num3) / 3;
    return avg;
}

const average = make_avg(1, 3, 5);
console.log('Average:', average);