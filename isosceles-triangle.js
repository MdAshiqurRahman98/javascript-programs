// Checking the Triangle Isosceles or Not using if-else

var side1 = 9;
var side2 = 8;
var side3 = 9;

if(side1 == side2 || side1 == side3 || side2 == side3) {
    console.log('The triangle is isosceles.');
}
else {
    console.log('The triangle is not isosceles.');
}