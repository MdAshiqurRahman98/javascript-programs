// Odd or Even Number using Function without return keyword

function odd_even(num) {

    if((num % 2) != 0) {
        console.log(num + ' is odd number.');
    }
    else {
        console.log(num + ' is even number.');
    }
}

odd_even(3);